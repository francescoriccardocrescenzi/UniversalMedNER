[project]
name = "universal-med-ner"
version = "0.0.0"

dependencies = [
    "torch==2.6.0",
    "torchvision",
    "transformers",
    "datasets",
    "trl",
    "peft",
    "huggingface_hub",
    "sacremoses",
    "numpy",
    "pandas",
    "scipy",
    "wandb"
]

[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"

[tool.uv.sources]
torch = { index = "pytorch-cu124" }