# UniversalMedNER

## Secrets

Add a `.env` file in the project root containing an `HF_TOKEN` field and a `WANDB_API_KEY` field 
containing a HugginFace token and a WandB API key respectively. The HugginFace token is supposed to be associated
with an account where the use of MedGemma has been enabled.